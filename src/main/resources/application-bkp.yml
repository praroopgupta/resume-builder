server:
  port: 8000
logging:
  level:
      org.springframework: INFO
spring:
  datasource:
      url: jdbc:mysql://${Local instance MySQL80:localhost}:3306/resume_builder
      username: root
      password: password
  jpa:
      hibernate.ddl-auto: update
  security:
    user:
        name: username
        password: password